import React, { Fragment } from "react";
import classes from "./Sale.module.css";

const Sale = (props) => {
  return (
    <Fragment>
      <section>
        <div className={classes.container}>
          <h1>판매 페이지</h1>
        </div>
      </section>
    </Fragment>
  );
};

export default Sale;
